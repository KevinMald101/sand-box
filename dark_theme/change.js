function dark() {
  document.body.style.backgroundColor = "black";
}

function light() {
  document.body.style.backgroundColor = "white";
}

function grey() {
  document.body.style.backgroundColor = "grey";
}

/*
function toggleDarkMode() {
  const darkMode = document.getElementById("darkMode");
  const lightMode = document.getElementById("lightMode");
  const darkModeClass = darkMode.className;
  const lightModeClass = lightMode.className;
  
  if (darkModeClass.indexOf("darkMode") > -1) {
    darkMode.className = "lightMode";
    lightMode.className = "darkMode";
  } else {
    darkMode.className = "darkMode";
    lightMode.className = "lightMode";
  }
}
*/